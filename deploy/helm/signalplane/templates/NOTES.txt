SignalPlane Silver has been installed.

Service:
  {{ include "signalplane.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

Check rollout:
  kubectl -n {{ .Release.Namespace }} rollout status statefulset/{{ include "signalplane.fullname" . }}

Port-forward for local access:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "signalplane.fullname" . }} 4318:{{ .Values.service.port }}

Then open:
  http://127.0.0.1:4318

Production reminder:
  Replace all default secret values before exposing this deployment.
